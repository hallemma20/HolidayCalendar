import './App.css'

function ConfigTab() {
  return (
    <section id="center">
      <h1>Holiday Calendar</h1>
      <div className="sso-card">
        <p>Channel setup is coming soon.</p>
        <p>Pinning a squad's calendar to this channel isn't built yet — check back after the full app ships.</p>
      </div>
    </section>
  )
}

export default ConfigTab
